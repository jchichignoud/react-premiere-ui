import React from "react";
import ReactDOM from "react-dom";
import Panel from "./Panel";
import "./variables.scss";

const rootElement = document.getElementById("root");
ReactDOM.render(<Panel />, rootElement);
